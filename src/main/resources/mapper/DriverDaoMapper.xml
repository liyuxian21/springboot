<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.driver.dao.DriverDao">
<!--    新增司机-->
    <insert id="addDriver" parameterType="com.driver.entity.DriverInfo">
        insert into t_sys_driver
        (
         driver_id,
         driver_num,
         provice_id,
         provice_name,
         city_id,
         city_name,
         area_id,
         area_name,
         user_id,
         is_deleted,
         create_people,
         create_time,
         update_people,
         update_time,
         version)
        values
        (
            #{driverId},
            #{driverCode},
            #{provienceId},
            #{provienceName},
            #{cityId},
            #{cityName},
            #{areaId},
            #{areaName},
            #{userId},
            0,
            #{createPeople},
            now(),
            #{updatePeople},
            now(),
            0)
    </insert>
<!--    新增司机-->
    <insert id="addDriver2" parameterType="com.driver.entity.DriverInfo">
        insert into t_sys_user
        (
         user_name,
         user_account,
         user_code,
         phone,
         id_card,
         user_pwd,
         role,
         is_deleted,
         create_people,
         create_time,
         update_people,
         update_time,
         version)
        values
        (
            #{driverName},
            #{driverAccount},
            #{userId},
            #{phone},
            #{idCard},
            #{password},
            3,
            0,
            #{createPeople},
            now(),
            #{updatePeople},
            now(),
            0)
    </insert>
<!--    查询司机详情-->
    <select id="findDriverDetailsById" parameterType="java.lang.String" resultType="com.driver.entity.DriverDetailVo">
        select
            a.driver_id  driverId,
            a.driver_num  driverCode,
            b.user_name  driverName,
            b.user_account driverAccount,
            b.phone       phone,
            b.id_card     idCard,
            a.provice_name provienceName,
            a.city_name     cityName,
            a.area_name     areaName
        from t_sys_driver a
        left join t_sys_user b on a.user_id=b.user_code
        where a.driver_id=#{driverId}
        and a.is_deleted=0
    </select>
<!--    修改司机-->
    <update id="updateDriverById" parameterType="com.driver.entity.DriverInfo">
        update t_sys_driver
        set
            driver_num      =#{driverCode},
            provice_id      =#{provienceId},
            city_id         =#{cityId},
            area_id         =#{areaId},
            update_people   =#{updatePeople},
            update_time     =now(),
            version         =version+1
        where driver_id=#{driverId}
        and version=#{version}
    </update>
    <update id="updateDriverById2" parameterType="com.driver.entity.DriverInfo">
        update t_sys_user
        set
            user_name       =#{driverName},
            phone           =#{phone},
            id_card         =#{idCard},
            update_people   =#{updatePeople},
            update_time     =now(),
            version         =version+1
        where user_code=#{userId}
        and version=#{version}
    </update>
<!--    查询司机列表-->
    <select id="listDriver" parameterType="com.driver.entity.DriverListVo" resultType="com.driver.entity.DriverListVo">
        select
            a.driver_id     driverId,
            b.user_name     driverName,
            a.driver_num    driverCode,
            b.user_account  driverAccount,
            b.phone         phone,
            b.id_card       idCard,
            a.provice_id  provienceId,
            a.city_id     cityId,
            a.area_id     areaId
        from t_sys_driver a
        left join t_sys_user b on a.user_id=b.user_code
        where a.is_deleted=0
        <if test="driverName != null and driverName != ''">
            and b.user_name like concat('%', #{driverName}, '%')
        </if>
        <if test="driverCode != null and driverCode != ''">
            and a.driver_num like concat('%', #{driverCode}, '%')
        </if>
        <if test="provienceId != null and provienceId != ''">
            and a.provice_id like concat('%', #{provienceId}, '%')
        </if>
        <if test="cityId != null and cityId != ''">
            and a.city_id like concat('%', #{cityId}, '%')
        </if>
        <if test="areaId != null and areaId != ''">
            and a.area_id like concat('%', #{areaId}, '%')
        </if>
        order by a.update_time desc
    </select>
<!--删除司机-->
    <delete id="deleteDriver" parameterType="java.lang.String">
        update t_sys_driver
        set
        is_deleted=1,
        update_time=now(),
        update_people=#{userId1},
        version=version+1
        where user_id in
        <foreach item="item" index="index" collection="listId" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>
    <delete id="delete" parameterType="java.lang.String">
        update t_sys_user
        set
        is_deleted=1,
        update_time=now(),
        update_people=#{userId1},
        version=version+1
        where user_code in
        <foreach item="item" index="index" collection="listId2" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>
</mapper>