<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.client.dao.ClientDao">
<!--    查询客户列表-->
    <select id="listClient" parameterType="com.client.entity.ClientInfo" resultType="com.client.entity.ClientInfo">
        select
            a.user_account  clientAccount,
            a.user_name     clientName,
            a.sex           sex,
            a.phone         phone,
            a.email         clientEmail,
            a.id_card       idCard,
            a.user_id       userId,
            a.version       version
        from
            t_sys_user a
        where
            a.is_deleted=0 and a.role=3
        <if test="clientName != null and clientName != ''">
            and a.user_name like concat('%', #{clentName}, '%')
        </if>
        <if test="clientAccount != null and clientAccount != ''">
            and a.user_account like concat('%',#{clientAccount},'%')
        </if>
        order by update_time desc
    </select>
</mapper>